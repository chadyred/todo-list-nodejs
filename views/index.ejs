<!DOCTYPE html>
<html lang="">
	<head>
		<meta charset="utf-8">
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<title>Title Page</title>

		<!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
		<!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
		<!--[if lt IE 9]>
			<script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
			<script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
		<![endif]-->
	</head>
	<body>
		<h1 class="text-center">Liste de tâches</h1>

		<div>
			<form action="/task/new/" method="post">
				<label for="task">Tâche: </label>
				<input type="text" name="task" id="task" value="" pattern="^(.*)$" title=""/>
				<input type="submit" value="Envoyer"/>
			</form>
		</div>

		<div class="task">
			Les taches à faire:
			<%
				if (typeof tasks != "undefined") {
			%>
				<ul>
					<%
						tasks.forEach(function(task, index) {
					%>
						<li>
							<!-- Pour le lien avec une boucle for, il faut faire un indexOf(tasks[i]) afin d'avoir le 
							véritable index qui n'est pas le témoin i ! -->
							<a href="/task/<%= index %>/delete/">X</a> <a href="/task/<%= index %>/edit/">✎</a>  <%= task %>
							<% 	
								console.log(tasks[index])
							%>
						</li>						

					<%

						});
					%>
				</ul>
			<%
				}
				else
				{
			%>
					Pas de tâche ! 
			<%
				}
			%>
		</div>
	</body>
</html>